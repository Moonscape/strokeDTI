name: stroke_dti
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11.4
  - ipykernel
  - pandas
  - statannotations
  - seaborn
  - plotly
  - pip
  - setuptools
  - pytorch=2.5.1
  - pip:
      - -e . # Install your package in editable mode
      - ./modified_packages/keggx # Path to the modified package
